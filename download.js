const download = require('download');
const decompress = require('decompress');

module.exports = async function download(os) {
    if(os === 'win32') {

    } else if (os === 'darwin') {

    } else if (os === 'linux') {
        
    }
}